# Add project specific ProGuard rules here.
# Release builds have minifyEnabled false above, so these rules aren't
# active by default -- this file is just here so Android Studio's default
# release build config has something to point at if you turn minification
# on later.
